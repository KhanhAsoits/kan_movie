import {ReviewTabScreen} from "../views/screens/ReviewTabScreen";
import {observer} from "mobx-react";

const ReviewTabViewModel = ({route, nav}) => {
    return (
        <ReviewTabScreen></ReviewTabScreen>
    )
}
export default observer(ReviewTabViewModel)